[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "plainpunch"
version = "1.0.0"
description = "A small self-hosted time clock without surveillance."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Logan Pendragon Multiverse" }]
dependencies = ["Flask>=3.1,<4", "gunicorn>=23,<24", "tzdata>=2025.2"]

[project.optional-dependencies]
dev = ["coverage[toml]>=7.10", "mypy>=1.17", "pytest>=8.4", "ruff>=0.12", "types-Flask>=1.1"]

[project.scripts]
plainpunch = "plainpunch.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/plainpunch"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["plainpunch"]

[tool.coverage.report]
fail_under = 90
show_missing = true

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["plainpunch"]
disallow_untyped_decorators = false
disallow_untyped_calls = false
disable_error_code = ["type-var"]
